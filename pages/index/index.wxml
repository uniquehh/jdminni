<!--index.wxml-->
<page-meta>
  <navigation-bar title=" ">
  </navigation-bar>
</page-meta>
<view class="index">
  <view class="search">
    <view class="search-box">
      <view class="inputbox">
      <image src="../../img/hsearch1.svg"></image>
        <input placeholder="游戏主机" type="text" />
      </view>
      <view class="btn">搜索</view>
    </view>
  </view>
  <view class="container">
    <view class="aside">
      <view class="scrollbox">
        <scroll-view
        scroll-y="true"
        enhanced="{{true}}"
        show-scrollbar="{{false}}"
        bounces="{{true}}"
        >
          <view class="menu" wx:for="{{aside}}" wx:key="index"
          bindtap="goodTypeClick"
          data-parent="{{item}}"
          >{{item.type_name}}
          </view>
        </scroll-view>
      </view>
    </view>
    <view class="main">
      <scroll-view
      scroll-y="true"
      >
        <view class="head">
          头部标题
        </view>
        <view class="wrap">
          <view class="list">
            <!-- <view class="itembox"
              wx:for="{{produce}}"
              wx:for-index="indexs"
              wx:for-item="items"
              wx:key="indexs"
            >
              <view>{{items.type}}</view>
              <navigator url="../hinfo/hinfo"
              class="item" wx:for="{{items.goods}}" wx:for-index="indexss"
              wx:for-item="itemss"
              wx:key="indexss"
              >
                <image aspectFit src="{{itemss.img}}"></image>
                <view>{{itemss.pname}}</view>
              </navigator>
            </view> -->
          </view>
        </view>
      </scroll-view>
    </view>
  </view>
</view>